#ReadMe
This program uses pythonsys, and webbrowser

USE:
To use, from the directory the file is saved in, run the file with keywords in the command line and it will either google or search stackoverflow and then return the top 5 results in it's own browser window. 

Example:

	lucky.py how heavy is my left hand


